import { RemoverGuionBajoPipe } from './remover-guion-bajo.pipe';

describe('RemoverGuionBajoPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoverGuionBajoPipe();
    expect(pipe).toBeTruthy();
  });
});
